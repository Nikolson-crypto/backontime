{
  "name": "backontime",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Parking-fine reminder PWA for Copenhagen: mark the car, set the limit, get told when to walk back",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "leaflet": "1.9.4"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "vitest": "^3.2.0"
  }
}
